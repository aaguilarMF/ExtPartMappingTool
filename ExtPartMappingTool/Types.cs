using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace ExtPartMappingTool
{
    public class Types
    {
        public enum ActionType
        {
            ADD = 1,
            SAVE = 2,
            DELETE = 3,
            UPDATE = 4,
            BEGIN_UPLOAD = 5,
            COMMIT_UPLOAD = 6
        }
        public enum ActionTypessssss
        {
            ADD = 1,
            SAVE = 2,
            DELETE = 3,
            UPDATE = 4,
            BEGIN_UPLOAD = 5,
            COMMIT_UPLOAD = 6
        }

    }
}